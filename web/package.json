{
  "name": "drug-interaction-screen-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.site.json",
    "test": "tsc -p tsconfig.json && node --test \"out/tests/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "typescript": "7.0.2"
  }
}
